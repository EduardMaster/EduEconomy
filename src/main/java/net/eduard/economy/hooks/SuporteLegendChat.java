package net.eduard.economy.hooks;

import net.eduard.api.lib.manager.EventsManager;

public class SuporteLegendChat extends EventsManager {


	/*
	@EventHandler
	public void chat(ChatMessageEvent e) {
		Player p = e.getSender();
		if (p.getName().equalsIgnoreCase(MagnataGerador.getPlayerMagnata())) {
			e.setTagValue("magnata", Main.getInstance().getConfigs().message("tag-magnata"));
		}

	}

	 */
}
